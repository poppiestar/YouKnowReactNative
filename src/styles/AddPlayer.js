
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: 'row'
    },
    input: {
        flex: 1
    },
    button: {
    }
});

export default styles;
